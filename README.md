# neo4j-graph-memory
A system for generating code with persistent memory. Tracks projects and their versions in a Neo4j graph database, enabling semantic search, history chaining, and reuse of code or designs from previous projects. Supports hybrid BM25 + vector search with cross-encoder reranking.
